{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "request": "launch",
            "name": "Build",
            "command": "npx yarn run start",
            "cwd": "${workspaceFolder}"
        }
    ]
}

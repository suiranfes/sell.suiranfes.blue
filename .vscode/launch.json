{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "Build",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
